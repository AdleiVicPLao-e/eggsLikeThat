{
  "name": "pet-game-server",
  "version": "1.0.0",
  "type": "module",
  "dependencies": {
    "axios": "^1.5.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "ethers": "^5.7.2",
    "express": "^4.18.2",
    "express-rate-limit": "^6.10.0",
    "helmet": "^7.0.0",
    "joi": "^17.9.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^7.5.0",
    "winston": "^3.10.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "blockchain:compile": "cd src/blockchain && npm run compile",
    "blockchain:deploy:amoy": "cd src/blockchain && npm run deploy:amoy",
    "blockchain:deploy:sepolia": "cd src/blockchain && npm run deploy:sepolia",
    "blockchain:deploy:local": "cd src/blockchain && npm run deploy:local",
    "blockchain:test": "cd src/blockchain && npm run test",
    "blockchain:node": "cd src/blockchain && npm run node"
  }
}
